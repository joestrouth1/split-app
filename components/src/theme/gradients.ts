import { colors } from './colors'

export const gradients = {
  primary: `linear-gradient(to bottom, ${colors.greens[4]}, ${colors.greens[5]})`,
  light: `linear-gradient(to bottom, ${colors.greens[0]}, ${colors.greens[2]})`,
}
